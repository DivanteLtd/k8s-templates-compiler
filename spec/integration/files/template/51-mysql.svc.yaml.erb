---

apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    application: mysql
  namespace: <%= values[:namespace][:name] %>
spec:
  ports:
  - port: 3306
    protocol: TCP
  selector:
    application: mysql
